# 03 — 16 周极致路线图（不计成本 · 质量优先）

> 比 12 周多 4 周缓冲：编译坑、注入返工、自用门禁。  
> **Solo 每天 ≥6h 或等价全职。** 每周五生产力仪式不可删。

---

## 总览

| Phase | 周 | 主题 | 出口门禁 |
|-------|-----|------|----------|
| **M0 地基** | W1–W2 | Fork · 编译 · DeepSeek 能干活 · 品牌最小 | 2 天真实自用 |
| **M1 安全** | W3–W4 | Lumen 安全基因完整落地 | smoke-security 全绿 |
| **M2 脑** | W5–W7 | Cache · Delivery · Goal · Storm · Coordinator | 5 天自用 + cache 可见 |
| **M3 反射** | W8–W9 | CC_PARITY · harness · 补洞 | 对照 ≥80% |
| **M4 魂** | W10–W12 | editverify · 垂直无缝 · eval | 自修演示 + 垂直 3 步 |
| **M5 打磨** | W13–W14 | UX 抛光 · 安装 · 文档 · 对比页 | 10 分钟新手路径 |
| **M6 发布** | W15–W16 | 15 天主用 · v1.0 · 法律终检 | 发布门禁全勾 |

**若某周「想切走 >2 次」→ 冻结新功能，只修体验，工期顺延（不计成本允许）。**

---

## M0 — 地基（W1–W2）

### 目标
本地 `lumen` 二进制 + DeepSeek 多轮 tool-use + 读/改/bash。

### 任务清单

| 日 | 任务 |
|----|------|
| D1 | 建 `~/code/lumen`；rsync grok-build → `agent/`；NOTICE/LEGAL/UPSTREAM |
| D2–D3 | `cargo check -p xai-grok-pager-bin` 排障（protoc/内存/jobs） |
| D4–D5 | `cargo build -p xai-grok-pager-bin --release` |
| D6 | bin 改名 `lumen`；关遥测；关 auto_update |
| D7 | DeepSeek config + 纯文本冒烟 |
| D8–D9 | tool：read + search_replace + bash E2E |
| D10 | welcome 文案 Lumen；`scripts/smoke-deepseek.sh` |
| D11–D12 | 真实项目用 2 天；写 journal |
| D13–D14 | 缓冲：登录/路径/CJK 卡点 |

### Exit
- [ ] `lumen --version`  
- [ ] DeepSeek 多轮 + edit + bash  
- [ ] 遥测/更新默认关  
- [ ] journal 连续 2 生产力日  

---

## M1 — 安全（W3–W4）

### 目标
L0–L3 全开；与 sandbox 叠加；**bypass 不能绕过 deny**。

### 任务
- 实现 `02-安全规格` 全节  
- `policy/GUARD_RULES.md` 与代码同步  
- `scripts/smoke-security.sh`  
- doctor 增加 security 自检（可选）  
- 审计 Grok 是否已有部分规则，**合并而非重复**

### Exit
- [ ] smoke-security 全绿  
- [ ] 三模式 writepath 测过  
- [ ] 分段 `&&` 测过  

---

## M2 — DeepSeek 脑 + 循环纪律（W5–W7）

### W5 Cache / 模型
- 状态栏 cache  
- presets：chat / reasoner / local  
- prefix 稳定审计文档  

### W6 Delivery + Goal
- 假完成 nudge  
- todos 未完拦截  
- strict 可选  

### W7 Storm + Coordinator
- 先 **grep/审计** Grok 是否已有 loop guard  
- 实现 Storm + RepeatSuccess  
- planner 只读 subagent 模板  
- **5 天自用门禁**

### Exit
- [ ] cache 用户可见  
- [ ] 假完成可演示拦截  
- [ ] storm 可演示  
- [ ] 5 生产力日  

---

## M3 — Claw 反射（W8–W9）

- 直接打开 **`policy/CC_PARITY.md`**（前 10 条已填），扩到 ≥40  
- 补缺失项 + 测试  
- **12 场景 = claw mock parity 改编**（`scripts/parity-run.sh`），**不计入** coding eval 20  
- 与 M1 安全交叉走查  

### Exit
- [ ] 完成率 ≥80%  
- [ ] parity-run 可重复  

---

## M4 — 魂（W10–W12）

### editverify
- 规格：`editverify/*` + 详细1 trait 思想  
- 落点：writer 批后钩子 / PostTool  
- Go → Python → JS  
- max_repair=3, timeout 可配  

### 垂直
- 迁 science/oasis/quant → packs  
- 安装器注册 MCP 或 wrapper 子命令  
- doctor 探测垂直健康  

### eval ≥20（来源写死 · 见 `policy/EVAL_BASELINE.md`）
- **Tier1 8：** 原样迁 `~/lumen/evals/tasks/01–08`  
- **Tier2 8：** 新建同格式（Py/TS/Go）  
- **Tier3 4：** 新建到 ≥20  
- **不是** claw mock parity（那是 M3）  
- DeepSeek 跑一轮 → `evals/BASELINE.md`  

### Exit
- [ ] Go 自修演示  
- [ ] 垂直 ≤3 步  
- [ ] coding eval ≥20 可跑 + 基线表  
  

---

## M5 — 打磨（W13–W14）

- UX 清单 A–F（见 04）全过  
- install.sh + README 10 分钟  
- 对比页：旧 Lumen / 裸 grok / 本品  
- 性能：长会话、大 diff 不崩  

---

## M6 — 发布（W15–W16）

- 累计 **15 工作日** 主用（可跨 M4–M6）  
- v1.0 tag + checksums  
- LEGAL 终检  
- 已知 P0 bug = 0  

### v1.0 门禁（全勾才可称发布）
- [ ] M0–M5 Exit 全过  
- [ ] 15 生产力日  
- [ ] smoke-deepseek + smoke-security  
- [ ] 10 分钟新手路径录像或逐步截图  
- [ ] NOTICE/LEGAL 正确  
- [ ] 作者声明：主用切换完成  

---

## 可选后期（v1.1+，不挡 v1.0）

| 项 | 说明 |
|----|------|
| M-rename | crate/路径 `xai-*` → `lumen-*` |
| Guardian LLM 审查 | 默认 off |
| 更强 memory | Reasonix memorycompiler 简化 |
| Windows 一等公民 | 若需要 |
| ACP 深度打磨 | Grok 已有，按编辑器需求 |

---

## 每周五仪式（永久）

```
1. scripts/smoke-deepseek.sh
2. scripts/smoke-security.sh   # M1 后强制
3. journal 本周回顾
4. 真实任务 1 次（非 smoke）
5. 想切走次数 >2？→ 下周只修体验
```
