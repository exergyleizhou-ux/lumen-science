# Day 0 progress — 2026-07-16

## Monorepo
- Path: `~/code/lumen`
- Foundation commit: `853a305` — chore: import grok-build as agent foundation (pinned Day 0)
- Layout: `agent/` (Grok Build pin), `packs/`, `policy/`, `docs/masterplan/`, `skills/`, `evals/`, `scripts/`, `journal/`, `NOTICE`, `LEGAL.md`

## cargo check
- Command: `cargo check -p xai-grok-pager-bin` (from `agent/`)
- Log: implementer scratch `lumen-check.log`
- First attempt: **FAIL** — `bin/protoc` is a dotslash launcher; `dotslash` missing; no `protoc` on PATH
- Fix applied: `brew install protobuf` → `/opt/homebrew/bin/protoc` (libprotoc 35.1); `export PROTOC=/opt/homebrew/bin/protoc`
- Second attempt: **PASS** — `Finished dev profile ... in 2m 25s`

## Next action (M0 continue)
1. Prefer documenting `PROTOC` in README / scripts for future builds
2. Optional: `cargo install dotslash` so tree-local `bin/protoc` works
3. DeepSeek-without-xAI-login: **still untested** (honest open item; not required for this Day 0 slice)
4. Later M0: release build, bin rename to `lumen`, telemetry/auto_update defaults off

## Productivity
- Not a product-use day yet — foundation only.
