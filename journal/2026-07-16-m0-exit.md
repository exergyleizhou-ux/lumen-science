# M0 exit note — 2026-07-16

## M0 checklist (shippable local body)

| Item | Status |
|------|--------|
| monorepo + Grok pin (`agent/`) | ✅ Day0 |
| `cargo check -p xai-grok-pager-bin` | ✅ |
| release build | ✅ `agent/target/release/lumen` |
| binary name `lumen` | ✅ `[[bin]] name = "lumen"` |
| DeepSeek default model | ✅ `default_models.json` → `deepseek-chat` |
| example base_url | ✅ `config/lumen.example.toml` |
| Mixpanel default off | ✅ `TelemetryConfig::default` |
| auto_update default off | ✅ `effective_auto_update(None) == false` |
| unit/structural tests | ✅ models/telemetry/update tests + `scripts/assert-defaults.sh` |
| smoke script | ✅ `scripts/smoke-deepseek.sh` (SKIP without key; live with key) |
| PROTOC documented | ✅ root README + smoke script |

## Explicitly **open** (not this goal)

- Multi-day self-use diary (M0 soft exit / M6)
- M1 security inject (5+1 / ZWSP / writepath)
- M2 Delivery/Goal/Storm/Coordinator product wiring
- M3 CC_PARITY ≥40 filled + parity-run
- M4 editverify + vertical packs + eval≥20
- M5 polish / M6 v1.0

## How to rebuild

```bash
export PROTOC=/opt/homebrew/bin/protoc
cd ~/code/lumen/agent && cargo build -p xai-grok-pager-bin --release
./target/release/lumen --version
```
