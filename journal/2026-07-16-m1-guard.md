# M1 progress — lumen-guard — 2026-07-16

## Done
- New crate `agent/crates/codegen/lumen-guard` (L0–L3 from ~/lumen/internal/guard)
- Wired into `permission/manager.rs` **before** YOLO / policy allow path
- `scripts/smoke-security.sh` + `policy/GUARD_RULES.md`
- Unit tests + masterplan minimum smoke cases in-crate

## Invariant
`Decision::PolicyDeny` with reason `lumen_guard_deny` — cannot be skipped by always-approve.

## Verify
```bash
./scripts/smoke-security.sh
```
