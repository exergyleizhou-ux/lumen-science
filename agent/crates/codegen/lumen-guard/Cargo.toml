[package]
name = "lumen-guard"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "Lumen L0–L3 safety guards (bash + writepath) — deny always wins over bypass"

[dependencies]
regex = { workspace = true }
once_cell = { workspace = true }
